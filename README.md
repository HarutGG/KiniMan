# ԿինոՄան

Cinematic movie & series finder (Armenian UI).

```bash
npm install
cp .env.example .env.local
# optional: add TMDB_API_KEY
npm run dev
```

Without a TMDB key the app uses a built-in mock catalog.
